services:
  - type: web
    name: yuzu-smash-ranking
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker --timeout 120 app:app
    envVars:
      - key: SECRET_KEY
        sync: false
