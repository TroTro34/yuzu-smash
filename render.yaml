services:
  - type: web
    name: yuzu-smash-ranking
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 --threads 8 --timeout 0 app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
