services:
  - type: web
    name: yuzu-smash-ranking
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 --worker-class eventlet --timeout 120 app:app
    envVars:
      - key: SECRET_KEY
        sync: false
